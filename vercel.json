{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "bash \"$(git rev-parse --show-toplevel)/scripts/vercel-install.sh\"",
  "buildCommand": "if [ \"$(basename $PWD)\" = Useronboarding ] || [ \"$(basename $PWD)\" = Agentdashboard2d3d ]; then cd ..; fi && bash scripts/vercel-build.sh",
  "outputDirectory": "Useronboarding/dist"
}
